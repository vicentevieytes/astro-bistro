---
// Recibe las propiedades necesarias
const { isOpen, onClose, onConfirm } = Astro.props;
---

{
    isOpen && (
        <div class="modal">
            <div class="modal-content">
                <span onclick={onClose} class="close">
                    &times;
                </span>
                <h2>¿Estás seguro de que deseas borrar este elemento?</h2>
                <button onclick={onConfirm}>Sí, Borrar</button>
                <button onclick={onClose}>Cancelar</button>
            </div>
        </div>
    )
}

<style>
    .modal {
        display: block; /* Mostrar por defecto */
        position: fixed;
        z-index: 1;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgba(0, 0, 0, 0.4);
    }

    .modal-content {
        background-color: #fefefe;
        margin: 15% auto;
        padding: 20px;
        border: 1px solid #888;
        width: 80%;
    }

    .close {
        color: #aaa;
        float: right;
        font-size: 28px;
        font-weight: bold;
    }

    .close:hover,
    .close:focus {
        color: black;
        text-decoration: none;
        cursor: pointer;
    }
</style>
